import { Loading } from '@/components/Loading'

/** Full-bleed like the table it is — same wait, same mark. */
export default function DiariaLoading() {
  return <Loading variant="screen" label="preparando a mão de hoje" />
}
